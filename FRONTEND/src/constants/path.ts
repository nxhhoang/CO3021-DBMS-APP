const path = {
  home: '/',
  user: '/user',
  profile: '/user/profile',
  login: '/login',
  register: '/register',
  logout: '/logout',
  // productDetail: ':nameId',
  // order: '/order'
} as const

export default path
